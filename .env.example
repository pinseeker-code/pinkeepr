# Pinkeepr Environment Configuration
# Copy this to .env and fill in your values
# SECURITY: chmod 600 .env after editing

# ============================================
# GATEWAY AUTHENTICATION
# ============================================
# Generate a secure random token:
#   openssl rand -hex 32
OPENCLAW_GATEWAY_TOKEN=your-secure-gateway-token-here

# ============================================
# NOSTR IDENTITY (Pinkeepr)
# ============================================
# Pinkeepr's private key (nsec or hex format)
# Generate with: nak key generate (or any Nostr key generator)
PINKEEPR_NSEC=nsec1your-private-key-here

# Pinkeepr's public key (for reference only, derived from nsec)
# PINKEEPR_NPUB=npub1your-public-key-here

# ============================================
# ORACLE (Your trusted identity)
# ============================================
# Your npub - Pinkeepr only responds to this pubkey
# Paste your npub from Damus/Amethyst/etc
ORACLE_NPUB=npub1your-oracle-public-key-here

# ============================================
# NOSTR RELAY
# ============================================
# Your private relay on Umbrel
# Use ws:// for local/Tailscale connections
NOSTR_RELAY_URL=ws://umbrel.local:4848

# ============================================
# OLLAMA (LLM Backend)
# ============================================
# Your laptop's Tailscale IP running Ollama
# Ollama doesn't require a real API key
OLLAMA_API_KEY=ollama-local
OLLAMA_HOST=http://100.79.12.80:11434

# Default model to use (must be pulled in Ollama first)
# Examples: llama3.3, qwen2.5-coder:32b, deepseek-r1:32b
DEFAULT_MODEL=llama3.3

# ============================================
# NETWORK BINDING
# ============================================
# Your Umbrel's Tailscale IP (bind gateway to this only)
# Leave as 127.0.0.1 for localhost-only, or use 0.0.0.0 for all interfaces
# SECURITY: Prefer Tailscale IP to avoid public exposure
TAILSCALE_IP=100.x.x.x

# ============================================
# OPTIONAL: Portainer API (for container control)
# ============================================
# If Pinkeepr needs to manage other containers:
# PORTAINER_API_URL=http://umbrel.local:9000/api
# PORTAINER_API_TOKEN=your-scoped-portainer-token

# ============================================
# OPTIONAL: Additional providers
# ============================================
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
